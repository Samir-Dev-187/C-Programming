// Delete an element of a given index from an array

#include<stdio.h>
int main()
{
    int n = 2;
    int arr[] = {1,2,3,4,5,6};

    for (int i = 0; i < 6; ++i)
    {
        if (i == n && i != 5)
        {
            arr[i] = arr[i+1];
        }
    }

    for (int i = 0; i < )

    return 0;
}